<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="toy.project.kando.user.mapper.UserMapper">
    <insert id="insertUser" parameterType="user">
        INSERT INTO USER
        (
          user_id
        , user_password
        , user_name
        , user_nick
        , user_email
        , user_type
        )
        VALUES
        (
          #{userId}
        , #{userPassword}
        , #{userName}
        , #{userNick}
        , #{userEmail}
        , #{userType}
        )
    </insert>
</mapper>